import { defineConfig } from 'vite';

export default defineConfig({
  base: '/7/',  // Your repository name for GitHub Pages
  root: '.',    // Make sure this is set to the correct root if using a custom directory structure
});
